#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false); cin.tie(NULL);
	freopen("flyingsafely.in", "r", stdin);

	int tc; cin >> tc;
	while (tc--) {
		int n,m; cin >> n >> m;
		while (m--) {
			int a,b; cin >> a >> b;
		}
		cout << n-1 << "\n";
	}

	return 0;
}