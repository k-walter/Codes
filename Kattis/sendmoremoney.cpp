#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false); cin.tie(NULL);
	freopen("sendmoremoney.in", "r", stdin);

	string line; getline(cin,line);
	for (char &c : line) {
		
	}

	return 0;
}